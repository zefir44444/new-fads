---
interface Props {
  date?: Date;
}

const { date = new Date() } = Astro.props;
---

<time datetime={date?.toISOString()}>
  {date?.toLocaleDateString('en-GB', {
    year: 'numeric',
    month: 'short',
    day: 'numeric',
  })}
</time>